# Keep real values in server/.env (ignored by Git), never in source code.
MONGO_URI=mongodb://127.0.0.1:27017/retalcare
PORT=5000
# Explicit maintenance only; do not enable on every Cloud Run instance.
RUN_INVOICE_MIGRATION=false
